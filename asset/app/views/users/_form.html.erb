<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :タイトル %>
    <%= form.text_field :title, id: :user_title %>
  </div>

  <div class="field">
    <%= form.label :内容 %>
    <%= form.text_area :contents, id: :user_contents %>
  </div>

  <div class="field">
    <%= form.label :画像 %>
    <%= form.file_field :image %>
  </div>

   <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :map_address %>
  </div>

  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
